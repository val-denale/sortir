{% extends '@partials/base.html.twig' %}

{% block title('Accueil') %}

{% block body %}
    <h1 class="text-center">Mon profil</h1>
    <div class="row mt-5">
        <div class="col-md-4">
            image ici
        </div>

        <div class="col-md-8">
            {{ form_start(form) }}
                {{ form_widget(form) }}

                <button type="submit" class="btn btn-primary">Enregistrer</button>
                <a href="{{ path('home') }}" class="btn-link">Annuler</a>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}